export type UsesendTransporterOptions = {
    /**
     * Your Usesend API key
     */
    apiKey: string;

    /**
     * Optional custom API endpoint URL
     * @example 'https://api.usesend.dev' (default)
     * @example 'https://your-custom-endpoint.com'
     */
    apiUrl?: string;
}